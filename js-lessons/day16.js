// Day 16 — JSON & Serialization
// Example:
const obj = { a: 1, b: 'two' };
const json = JSON.stringify(obj);
console.log('json:', json);
const parsed = JSON.parse(json);
console.log('parsed:', parsed);
// Exercise: store a nested object as JSON and retrieve a nested value.
