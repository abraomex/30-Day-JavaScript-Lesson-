// Day 08 — Array Methods (map, filter, reduce)
// Example:
const arr = [1, 2, 3, 4, 5];
console.log('map *2:', arr.map(x => x * 2));
console.log('filter odd:', arr.filter(x => x % 2 === 1));
console.log('sum reduce:', arr.reduce((s, v) => s + v, 0));
// Exercise: use reduce to compute product of array elements.
