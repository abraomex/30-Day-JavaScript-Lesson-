// Day 05 — Functions (declaration, expression, return)
// Example:
function add(x, y) {
  return x + y;
}
const result = add(2, 3);
console.log('add(2,3)=', result);

// function expression
const mul = function (x, y) {
  return x * y;
};
console.log('mul(3,4)=', mul(3, 4));
// Exercise: write a function that checks if a string is a palindrome.
